import org.junit.Test
import kotlin.test.assertEquals

class ParserTests {

    @Test
    fun `one of the tests`() {
        assertEquals(
            expected = "1",
            actual = "1"
        )
    }
}